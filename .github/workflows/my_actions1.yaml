name: Creative workflows1

on:
   push:
     branches:
          -master
        workflow_dispatch:
jobs:
    perform-action:
          runs-on: ubuntu-latest
          steps:
         -name: Greetings
          run: echo "Hello developer, u have pushed successfully."
          shell: bash
